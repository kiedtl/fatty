Showstoppers
x cd
x update listing with inotify
- pty for each execution
  - properly closing ptys
  - jobs writing to a pty properly
- Ctrl+Z, fg, bg
x terminal input

Basic functions
- input
  x auto-focus on startup
  x basic vim editing
  - ctrl+l to "clear"
- statusbar
  x current directory
  - git branch
  x hostname? (why bother since you can only be local?)
x read from pty master properly
- history
  x arrow keys to go back/forwards
  - time began, ended, taken
  - directory

Shell
- builtin: select
  x basic
  - Exclusion mode flag -e
x builtin: where filter
- builtin: foreach/map
x index array/table with arbitrary expressions
x variable assignments
  x array literals
  x table literals
  - datetime literals
    - datetime comparisons
x assertions
x tests
x literals for true, false, nil, undefined
- set variables to output of command
  x object output
  - stdout
x expressions in commands
- combination <> operator
- environment variables
  - $env variables
  - set for command
- range type
  - Iterating over range
  - Indexing by range

Shell: figure out uses for new syntax
- command!
- @command

Shell: various builtins/tools
- format (format date, time, duration, size, ...)
- parse (parse date, time, duration, integer, float, size, ip address, file mode)
- sort-by (ascending, descending)

QoL
- Put large tables in scrollbar
- Show command invocation in UI
  - Marish: track LineCol for commands
  - Show both user input and what was actually run

Crickhollow (re)implementations
- document output
  x cp
    - progress bars
  x du
    - FATTY env var
    - spinner
  - top
    - graphs
  - pastel
  - cal
  - qr (great for checking images...)
  - cat
    - markdown
    - code (bat syntax highlighting?)
    - images
- object output
  x df
  x ps
  - ls
  x du
  - wc
  - lsblk
  - sqlite repl
  - fd (wrapper)
  - rg (wrapper)
  - tar (lmao)
    - See: doc/fatty-todo.txt

Reimplementation & new utility priorities
- 1 gpuinfo: lspci | grep -i vga
- 1 cpuinfo: lscpu

Project: completions

Project: user input
x regular terminal input
- document input
  - once done: fork doas
    - https://github.com/multiplexd/doas/blob/master/doas.c

Project: object output
- Crickhollow: object manipulation (or built in?)
  - Accessing fields
  - Accessing nth item, range of items
  - Mapping over range of items

Project: convert bolger to binary format

Nice to have
- Listing: show git status
